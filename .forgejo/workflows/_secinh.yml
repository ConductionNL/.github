# TEMP caller: compare inherit vs explicit secret delivery into a reusable. DELETE after.
name: _secinh
on:
  workflow_dispatch: {}
jobs:
  via_inherit:
    uses: ./.forgejo/workflows/_secinh-target.yml
    secrets: inherit
  via_explicit:
    uses: ./.forgejo/workflows/_secinh-target.yml
    secrets:
      CODEBERG_TOKEN: ${{ secrets.CODEBERG_TOKEN }}
