# TEMPORARY CI self-test target — diagnoses reusable workflow_call runs-on
# propagation on Codeberg Forgejo. Runs only when called. DELETE after diagnosis.
name: _ci-selftest-target

on:
  workflow_call:
    inputs:
      caller:
        required: false
        type: string
        default: "?"

jobs:
  echo:
    name: target-echo
    runs-on: codeberg-small
    container:
      image: node:24-bookworm
    steps:
      - run: echo "reusable target ran via ${{ inputs.caller }} on codeberg-small"
