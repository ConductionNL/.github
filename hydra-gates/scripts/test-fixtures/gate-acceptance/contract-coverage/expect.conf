# expect.conf — read by scripts/lib/test_gate_acceptance_matrix.sh
#
# Bundle: contract-coverage — gate-25, .github#363.
#
# The two arms differ ONLY in the test file: the controller is byte-identical
# in both. That is deliberate — it makes this pair a test of WHICH METHOD the
# gate binds an auth attribute to, not of whether it can count.
#
#   planted/  one test, covering listThings. farAttribute is public and
#             untested, so the gate MUST name it. Against the twenty-line
#             window it did not: farAttribute's own #[PublicPage] is 21 lines
#             above its declaration, so the gate saw a non-public method and
#             said nothing. THE FALSE NEGATIVE.
#
#   clean/    both public endpoints tested; adminOnlyPurge still has no test
#             and still has no attribute of its own. Against the twenty-line
#             window this arm FAILS, because the window reaches back over
#             listThings()'s closing brace and credits adminOnlyPurge with
#             listThings()'s #[PublicPage]. THE FALSE POSITIVE.
#
# So a gate that merely got quieter cannot satisfy this bundle: the planted arm
# requires it to report MORE (farAttribute) and the clean arm requires it to
# report LESS (not adminOnlyPurge), and both are graded on the same controller.
gate 25 hydra-gate-contract-coverage.log FAIL PASS thing#farAttribute
