A repository that ships no flow nodes of its own.

Gate-111 must report NOT APPLICABLE here (rc 4), which is neither a pass nor a
failure — see `../expect.conf` for why that arm cannot live in the matrix.
