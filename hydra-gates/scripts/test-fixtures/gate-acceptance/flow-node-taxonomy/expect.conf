# expect.conf — read by scripts/lib/test_gate_acceptance_matrix.sh
#
# Bundle: flow-node-taxonomy — gate-111.
#
# Both arms are a repository that ships its OWN flow nodes, so both are in
# scope. planted/ adds a node declaring no kind or category and must FAIL;
# clean/ declares on every node and must PASS.
#
# 🔑 THE PLANTED ARM KEEPS A DECLARED NODE BESIDE THE PLANTED ONE, so the gate
# is proven to refuse a NODE rather than a directory. A checker that failed the
# whole tree would pass a single-file fixture for free.
#
# ⚠️ THE THIRD VERDICT IS NOT HERE, and cannot be. A repository with no
# lib/Service/Flow/Nodes at all must report NOT APPLICABLE — which is neither
# FAIL nor PASS, and is the arm that matters most: 38 of 65 step types on a
# measured instance are contributed by apps in OTHER repositories, and a gate
# scoped by interface rather than by path would fire on every one of them in
# pull requests that cannot fix them. That arm lives in
# scripts/lib/test_gate111_flow_node_taxonomy.sh.
gate 111 hydra-gate-flow-node-taxonomy.log FAIL PASS PlantedNode
