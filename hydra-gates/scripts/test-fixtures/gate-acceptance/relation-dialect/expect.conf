# expect.conf — read by scripts/lib/test_gate_acceptance_matrix.sh
#
# Bundle: relation-dialect — gate-54 (ADR-062 rule 7).
#
# planted/ carries the retired per-schema `x-openregister-relations` block;
# clean/ expresses the same relation the canonical way, as a property-level
# $ref that resolves inside the register set. The two files are otherwise
# identical, so the clean arm also proves the canonical dialect is not itself
# tripping the gate.
#
# This bundle runs WITHOUT a git history, so no base ref exists and the
# INHERITED / INTRODUCED labelling is not exercised here — that needs two
# commits and is asserted in test_gate_tag_binding_and_provenance.sh instead.
# What this pair pins is that the labelling did not change WHICH findings the
# gate reports, which is the way a "make it readable" change quietly becomes a
# "make it quieter" change.
gate 54 hydra-gate-relation-dialect.log FAIL PASS x-openregister-relations
