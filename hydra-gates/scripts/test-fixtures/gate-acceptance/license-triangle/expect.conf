# expect.conf — read by scripts/lib/test_gate_acceptance_matrix.sh
#
# Bundle: license-triangle — gate-28.
#
# composer.json declares EUPL-1.2 in BOTH arms; only the per-file @license
# moves. planted/ says MIT and must FAIL; clean/ agrees and must PASS.
#
# The clean arm carries a second job. gate-28's chain reaches its
# `SKIPPED (structural) — ... not one carried an @license` branch whenever the
# compared-file count is zero, and that sentence is a claim about the
# REPOSITORY. This file plainly carries a declaration, so any run that reports
# it as carrying none has said something false — which is exactly what a
# crashed helper used to make it say (see
# test_gate_crashed_checker_is_not_a_finding.sh, gates 22/25/28 arm).
gate 28 hydra-gate-license-triangle.log FAIL PASS lib/Service/LicenceService.php
