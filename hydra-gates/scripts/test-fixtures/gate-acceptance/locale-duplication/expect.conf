# expect.conf — read by scripts/lib/test_gate_acceptance_matrix.sh
#
# Bundle: locale-duplication — gate-103 (locale-duplication).
#
# The two arms differ in ONE thing: whether hr.js holds its own translation or a
# byte-for-byte copy of cs.js. en.js and cs.js are identical between the arms,
# and both arms carry 105 translated values — above the checker's MIN_VALUES
# floor of 100, so the planted arm fails on the copy rather than on being small.
#
# THE CLEAN ARM IS THE LOAD-BEARING HALF. Bosnian and Croatian sit around 96%
# identical in a genuinely translated bundle, and keepiq's language families sit
# at 78-89% — both are real translations, neither is a copy. A gate keyed on a
# PERCENTAGE would redden them. This fixture pins the discrimination the gate
# actually makes: exact equality across every translated value, which two
# independent translations of 105 strings never produce.
#
# The subject is the pair, not a count, because "1 duplicate pair" tells nobody
# which two languages a user is being served interchangeably.
gate 103 hydra-gate-locale-duplication.log FAIL PASS cs and hr
